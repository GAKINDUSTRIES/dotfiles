# This needs to be loaded in .zshrc_after cause rvm needs to be in PATH at first place. rvm script loads it in PATH env variable.

# Load RVM into a shell session *as a function*
# [[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm"
